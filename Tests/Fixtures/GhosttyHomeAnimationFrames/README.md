These frames were copied from the Ghostty website repository (`website/terminals/home/animation_frames`).

Upstream source: Ghostty website terminal home animation assets.

Upstream license: MIT.

Notes:
- The raw `frame_*.txt` files are preserved as vendored test fixtures.
- Glassdeck parses the existing `<span class="b">...</span>` markup into ANSI bold runs at load time.
- These fixtures are used for the internal animation demo scenario and the Ghostty home animation performance tests.
